# -*- mode: qore; indent-tabs-mode: nil -*-
# test workflow definition
#
# Qorus Integration Engine

%new-style

our string format_version = "2.6";

our hash<auto> groups = {
    "demo": {"desc": "demo interfaces"},
    "demo-erp": {"desc": "ERP demo interfaces"},
};

our list<auto> steps = (
    {
        "name": "erp_interorg_transfer_validate_in:1.0",
        "desc": "validates inter-org transfer information and compares it to the actual stock status",
        "funcname": "demo:1.0",
        "valname": "demo_val:1.0",
    },
    {
        "name": "erp_interorg_transfer_import_in:1.0",
        "desc": "imports the validated inter-org transfer data",
        "funcname": "demo:1.0",
        "valname": "demo_val:1.0",
    },
    {
        "name": "erp_interorg_transfer_finalize_in:1.0",
        "desc": "sends a notification to the internal logistics group about inter-org transfer processing (success or error details, if any)",
        "funcname": "demo:1.0",
        "valname": "demo_val:1.0",
    },
);

our hash<auto> workflows."ERP-BATCH-SCM-INTERORG-TRANSFER-IN"."1.0" = {
    "desc": "Demo ERP Supply Chain Management inter-org transfer import workflow; imports movements of goods between stores to align ERP with the actual stock locations",
    "author": "Qore Technologies, s.r.o.",
    "steps": steps,
    "groups": ("demo", "demo-erp"),
    "sla_threshold": 1,
    "autostart": 1,
    "remote": False,
};
