# -*- mode: qore; indent-tabs-mode: nil -*-
# test workflow definition
#
# Qorus Integration Engine

%new-style

our string format_version = "2.6";

our hash<auto> groups = {
    "demo": {"desc": "demo interfaces"},
    "demo-erp": {"desc": "ERP demo interfaces"},
};

our list<auto> steps = (
    {
        "name": "erp_po_receipt_validate_in:1.0",
        "desc": "validates PO receipt information and compares it to the original request",
        "funcname": "demo:1.0",
        "valname": "demo_val:1.0",
    },
    (
        {
            "name": "erp_po_receipt_import_in:1.0",
            "desc": "imports the validated PO receipt data",
            "funcname": "demo:1.0",
            "valname": "demo_val:1.0",
        },
        {
            "name": "erp_po_receipt_in_error_out:1.0",
            "desc": "returns errors to the external logistics provider, if any are found",
            "funcname": "demo:1.0",
            "valname": "demo_val:1.0",
        },
    ),
    {
        "name": "erp_po_receipt_finalize_in:1.0",
        "desc": "sends a notification to the internal logistics group about PO receipt processing (success or error details, if any)",
        "funcname": "demo:1.0",
        "valname": "demo_val:1.0",
    },
);

our hash<auto> workflows."ERP-BATCH-P2P-PO-RECEIPT-IN"."1.0" = {
    "desc": "Demo ERP Procure To Pay PO receipt import workflow; imports PO receipt info as received from logistics partners",
    "author": "Qore Technologies, s.r.o.",
    "steps": steps,
    "groups": ("demo", "demo-erp"),
    "sla_threshold": 1,
    "autostart": 1,
    "remote": False,
};
