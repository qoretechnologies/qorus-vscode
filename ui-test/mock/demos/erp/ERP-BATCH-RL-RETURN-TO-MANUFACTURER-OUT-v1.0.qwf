# -*- mode: qore; indent-tabs-mode: nil -*-
# test workflow definition
#
# Qorus Integration Engine

%new-style

our string format_version = "2.6";

our hash<auto> groups = {
    "demo": {"desc": "demo interfaces"},
    "demo-erp": {"desc": "ERP demo interfaces"},
};

our list<auto> steps = (
    {
        "name": "erp_rtm_check_staging_out:1.0",
        "desc": "check staging tables for output data & lock for outbound processing",
        "funcname": "demo:1.0",
        "valname": "demo_val:1.0",
    },
    {
        "name": "erp_rtm_transform_send_data_out:1.0",
        "desc": "retrieve, transform, and send the outbound data",
        "funcname": "demo:1.0",
        "valname": "demo_val:1.0",
    },
    {
        "name": "erp_rtm_finalize_out:1.0",
        "desc": "update ERP system that the data has been transferred",
        "funcname": "demo:1.0",
        "valname": "demo_val:1.0",
    },
);

our hash<auto> workflows."ERP-BATCH-RL-RETURN-TO-MANUFACTURER-OUT"."1.0" = {
    "desc": "Demo ERP Reverse Logistics PO return to manufacturer export workflow; exports an inter-org transfer of goods from items already imported into the central warehouse stock",
    "author": "Qore Technologies, s.r.o.",
    "steps": steps,
    "groups": ("demo", "demo-erp"),
    "sla_threshold": 1,
    "autostart": 1,
    "remote": False,
};
