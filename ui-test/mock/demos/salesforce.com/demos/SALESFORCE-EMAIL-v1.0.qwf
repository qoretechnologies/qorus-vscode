# -*- mode: qore; indent-tabs-mode: nil -*-

$format_version = "2.6";

$groups.DEMO.desc = "demonstration code";

const get_account = (
    "name": "sf_email_get_account:1.0",
    );

const send_email = (
    "name": "sf_email_send_email:1.0",
    );

$steps = (
    get_account,
    send_email,
    );

$workflows."SALESFORCE-EMAIL"."1.0" = (
    "desc": "Salesforce.com account to email demo",
    "author": "Qore Technologies, s.r.o.",
    "steps": $steps,
    "functions": "demo-lib",
    "groups": ("DEMO"),
    "keylist": ("account"),
    "onetimeinit": "sf_email_init:1.0",
    "mappers": (
        "salesforce-email:1.0",
    ),
    "autostart": 1,
    );
