# -*- mode: qore; indent-tabs-mode: nil -*-

# service: supplier-soap
# serviceversion: 1.0
# servicedesc: Supplier SOAP service
# serviceauthor: Qore Technologies, s.r.o.
# autostart: true
# parse-options: PO_NEW_STYLE, PO_REQUIRE_TYPES, PO_STRICT_ARGS
# define-group: DEMO: demonstration code
# groups: DEMO
# resource: supplier.wsdl
# ENDSERVICE

# name: init
# desc: initializes the service
sub init() {
    svc_register_soap_handler(("wsdl_resource": "supplier.wsdl"));
}
# END

# desc: receives stock level info for MIP 41 and creates a transfer order to push the data to iSEPL for processing
hash sub createSupplier(softlist l) {
    log(LL_INFO, "got %d supplier%s", l.size(), l.size() == 1 ? "" : "s");
    return (
        "Result": (
            "status": "OK",
        ),
        );
}
# END
