%new-style
%require-types
%strict-args
%enable-all-warnings

class TelcoUpdateAccountKey inherits Demo {
    primary() {
        *list<auto> account_keys = getChildFeedback().values();
        if (account_keys) {
            setOrderKey("account_id", account_keys);
        }
    }
}
