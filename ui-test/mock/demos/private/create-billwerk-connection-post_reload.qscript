#!/usr/bin/env qore

%new-style
%strict-args
%require-types
%enable-all-warnings

%requires QorusClientCore

QorusClient::init2();

# update REST connection
const Conn = "billwerkrests-demo";
hash<auto> opts = {
    "swagger": normalize_dir(get_script_dir() + DirSep + "billwerk-v1-swagger.yaml"),
    "client_id": "5e786ce64de084131881ed8e",
    "client_secret": "90da27cf63cee0249588584204f65a83",
};
qrest.put("remote/user/" + Conn, {"opts": opts});
printf("updated %y with options: %y\n", Conn, opts);